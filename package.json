{
  "name": "lookup-service-level-1",
  "version": "1.0.0",
  "scripts": {
    "build": "dotnet build app/backend.sln",
    "start": "cd app/backend && dotnet run",
    "test": "cypress run"
  },
  "devDependencies": {
    "cypress": "^12.17.1"
  }
}
